import {Component, Input, OnInit} from '@angular/core';

@Component({
  selector: 'app-layer-badge',
  templateUrl: './layer-badge.component.html',
  styleUrls: ['./layer-badge.component.scss']
})
export class LayerBadgeComponent implements OnInit {
  constructor() { }
  // @Input() color: 'primary' | 'accent' | 'accent-purple' | 'warn' = 'primary';

  ngOnInit(): void {
  }

}
